import 'package:flutter/material.dart';

class News24 extends StatelessWidget {
  const News24({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}